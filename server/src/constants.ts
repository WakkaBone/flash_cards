export const APIS = {
  auth: "auth",
  cards: "cards",
  version: "version",
};

export const COLLECTIONS = {
  users: "users",
  cards: "cards",
};

export enum STATISTICS_ACTIONS {
  Correct = "correct",
  Wrong = "wrong",
}
